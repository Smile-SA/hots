"""
=========
cots allocation
=========

Provide resource allocation related functions to handle this problem.
"""


def constraints_satisfied(config) -> bool:
    """Check if allocation constraints are satisfied or not."""
    satisfied = True
    print(config)
    print(type(config))

    return satisfied
